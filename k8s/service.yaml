apiVersion: v1
kind: Service
metadata:
  name: ml-api
  namespace: mlops-demo
  labels:
    app: ml-api
spec:
  type: NodePort
  selector:
    app: ml-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080